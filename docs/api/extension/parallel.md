# Parallel


Use `parallel()` for explicit durable branches and configurable batch
completion behavior.

::: async_durable_execution
    options:
      members:
        - CompletionReason
        - CompletionStatus
        - CompletionDecision
        - NestingType
        - CompletionConfig
        - BatchItemStatus
        - BatchItem
        - BatchResult
        - parallel
