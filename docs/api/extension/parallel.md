# Parallel

Internal implementation module: `async_durable_execution._operation.parallel`.

Use `parallel()` for explicit durable branches and configurable batch
completion behavior.

::: async_durable_execution._operation.parallel
    options:
      members:
        - CompletionReason
        - CompletionStatus
        - CompletionDecision
        - NestingType
        - CompletionConfig
        - BatchItemStatus
        - BatchItem
        - BatchResult
        - parallel
