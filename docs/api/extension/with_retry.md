# Retry Durable Work

Implementation module: `async_durable_execution.extension.with_retry`.

Use `with_retry()` to retry a block that can contain multiple durable
operations.

::: async_durable_execution.extension.with_retry
    options:
      members:
        - WithRetryContext
        - with_retry
