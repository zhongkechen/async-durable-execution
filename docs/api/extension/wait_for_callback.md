# Wait for Callback


Use `wait_for_callback()` to create a callback, run a submitter step, and wait
for an external result as one reusable operation.

::: async_durable_execution
    options:
      members:
        - wait_for_callback
        - WaitForCallbackContext
        - get_wait_for_callback_context
