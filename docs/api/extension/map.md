# Map

Implementation module: `async_durable_execution.extension.map`.

Use `map()` for durable item fan-out with bounded concurrency, completion
policies, and aggregated results.

::: async_durable_execution.extension.map
    options:
      members:
        - MapItemContext
        - map
