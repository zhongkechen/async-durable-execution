# Current Context

Implementation module: `async_durable_execution.context`.

Use `get_current_context()` only while supported durable user code is running.

::: async_durable_execution.context
    options:
      members:
        - DurableContext
        - get_current_context
