# Wait

Internal implementation module: `async_durable_execution._primitive.wait`.

Use `wait()` to suspend a durable execution without consuming Lambda compute
for the delay.

::: async_durable_execution._primitive.wait
    options:
      members:
        - wait
