# Wait

Implementation module: `async_durable_execution.primitive.wait`.

Use `wait()` to suspend a durable execution without consuming Lambda compute
for the delay.

::: async_durable_execution.primitive.wait
    options:
      members:
        - wait
