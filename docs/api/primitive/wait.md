# Wait

Operation module: `async_durable_execution._operation.wait`.

Use `wait()` to suspend a durable execution without consuming Lambda compute
for the delay.

::: async_durable_execution._operation.wait
    options:
      members:
        - wait
