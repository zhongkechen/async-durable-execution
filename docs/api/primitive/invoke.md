# Invoke

Implementation module: `async_durable_execution.primitive.invoke`.

Use `invoke()` to call another qualified durable Lambda function and wait for
its durable result.

::: async_durable_execution.primitive.invoke
    options:
      members:
        - invoke
