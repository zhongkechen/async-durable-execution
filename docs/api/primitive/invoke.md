# Invoke

Operation module: `async_durable_execution._operation.invoke`.

Use `invoke()` to call another qualified durable Lambda function and wait for
its durable result.

::: async_durable_execution._operation.invoke
    options:
      members:
        - invoke
