# Invoke

Internal implementation module: `async_durable_execution._primitive.invoke`.

Use `invoke()` to call another qualified durable Lambda function and wait for
its durable result.

::: async_durable_execution._primitive.invoke
    options:
      members:
        - invoke
